# 按钮

## 演示

:::demo ./Basic

基础用法

基础用法

:::

:::demo ./DisableElevation

禁止阴影效果

仅对`variant=contained`会有阴影效果

:::

:::demo ./DisableRipple

禁止水波纹效果

禁用后点击时不会出现水波纹效果

:::

:::demo ./Disabled

禁用按钮

禁用按钮时，对于`variant=contained`的水波纹效果和阴影都会失效

:::

:::demo ./Size

不同尺寸

通过`size`设置不同尺寸的按钮

:::

:::demo ./Loading

加载中

加载中状态用于需要一段时间处理后才能继续操作的场景。

:::

:::demo ./Link

跳转按钮

通过给按钮设置`href`，可以使按钮获得`a`标签的能力

:::

:::demo ./Long

长按钮

通过给按钮设置`long`，可以使按钮占据一整行

:::

## Button API

> 其余参数请参考[BaseButton](/components/BaseButton)

| 参数 | 说明 | 类型 | 可选值 | 默认值 | 是否必填 |
| --- | --- | --- | --- | --- | --- |
| href | 点击跳转地址，此时 button 行为与 a 标签一致 | string | - | - | false |
| disabled | 禁止按钮 | boolean | - | false | false |
| color | 设置按钮主题 | string | default &#x007c; primary &#x007c; success &#x007c; warning &#x007c; error &#x007c; secondary | default | false |
| variant | 设置按钮类型 | string | contained &#x007c; outlined &#x007c; text | contained | false |
| long | 设置为长按钮 | boolean | - | false | false |
| prefixIcon | 前置图标 | ReactElement | - | - | false |
| suffixIcon | 后置图标 | ReactElement | - | - | false |
| loading | 设置按钮为加载中 | boolean | - | - | false |
| round | 圆角按钮 | boolean | - | - | false |
| disableElevation | 禁止阴影效果，仅对`variant=contained`有效 | boolean | - | - | false |
| children | 子节点，在指定了`prefixIcon`或`suffixIcon`时可以为空 | ReactNode | - | - | false |
| size | 设置按钮大小 | string | middle &#x007c; large &#x007c; small | middle | false |
