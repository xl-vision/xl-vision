import { Locale } from './types';

const locale: Locale = {
  Popconfirm: {
    cancelText: '取消',
    confirmText: '确认',
  },
  Dialog: {
    cancelText: '取消',
    confirmText: '确认',
  },
};

export default locale;
