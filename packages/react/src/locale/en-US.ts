import { Locale } from './types';

const locale: Locale = {
  Popconfirm: {
    cancelText: 'No',
    confirmText: 'Yes',
  },
  Dialog: {
    cancelText: 'No',
    confirmText: 'Yes',
  },
};

export default locale;
