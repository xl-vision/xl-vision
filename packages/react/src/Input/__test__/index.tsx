// import React from 'react';
// import { mount } from 'enzyme';
// import Input from '..';

describe('Input', () => {
  it.todo('test onChange event', () => {
    // const handleChange = jest.fn();
    // const wrapper = mount(<Input onChange={handleChange} />);
    // wrapper.find('input').simulate('keypress', { key: 'a' });
    // console.log(wrapper.find('input').debug());
    // expect(handleChange.mock.calls).toBe('a');
  });
});
