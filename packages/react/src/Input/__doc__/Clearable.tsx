import { Input } from '@xl-vision/react';

const Demo = () => {
  return (
    <div>
      <Input placeholder='Basic usage' defaultValue='hello' allowClear={true} />
    </div>
  );
};

export default Demo;
