'use client';

import { Input } from '@xl-vision/react';

const Demo = () => {
  return (
    <div>
      <Input allowClear={true} defaultValue='hello' placeholder='Basic usage' />
    </div>
  );
};

export default Demo;
