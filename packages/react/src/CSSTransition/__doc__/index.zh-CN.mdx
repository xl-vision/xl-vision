# CSS过渡动画

::: demo ./Basic

基本用法

基本的使用方式，如果不希望一开始组件就渲染出来，请设置`mountOnEnter=true`

:::

::: demo ./Advance

不卸载组件

如果希望组件出场后不卸载组件，可以通过不设置`unmountOnLeave`，配合 css 实现

:::

## CSSTransition API

> CSSTransition 继承所有[Transition](/components/Transition)属性

| 参数 | 说明 | 类型 | 可选值 | 默认值 | 是否必填 |
| --- | --- | --- | --- | --- | --- |
| transitionClasses | 指定动画过程中的`class`名 | string &#x007c; object | - | - | false |
| timeout | 指定动画需要的时间 | string &#x007c; object | - | - | false |
| disableCss | 禁用 css 动画，如果想使用 js 进行动画控制，避免 css 动画的干扰，可以将此属性置为`true` | boolean | - | - | false |
